## Setup

Clone the repository:
```bash
git clone https://github.com/KaeL-0/Panic-TheCisco-VAMS
cd your-repo

Install dependencies:
npm install


Run code:
npm run dev

Open the app in your browser at the URL shown in the terminal (usually http://localhost:5173).