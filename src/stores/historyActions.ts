// This file has been intentionally removed.
// historyActions.ts was entirely dead code — none of its four exports
// (createTransformAction, createVertexPositionAction, createTextEditAction,
// applyColorPresetToVertices) were imported or called anywhere in the codebase.
// All action stubs had empty end() implementations with no revert logic.
// History is managed directly via pushToHistory() in each slice action.
export {};
